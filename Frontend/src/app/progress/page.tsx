import { Suspense } from "react";
import { SupportingRoute } from "@/components/supporting-route";
export default function ProgressPage() { return <Suspense><SupportingRoute kind="progress" /></Suspense>; }
