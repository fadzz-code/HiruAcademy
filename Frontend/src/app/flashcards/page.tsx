import { Suspense } from "react";
import { FlashcardCollection } from "@/components/flashcard-collection";

export default function FlashcardsPage() { return <Suspense><FlashcardCollection /></Suspense>; }
