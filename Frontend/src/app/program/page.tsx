import Link from "next/link";
import { PublicPage } from "@/components/public-shell";
import { levelCatalog, plans } from "@/lib/public-mock";

export default function ProgramPage() { return <PublicPage active="Program"><main className="public-main"><section className="public-hero"><p className="kicker">PROGRAM & LEVEL</p><h1>Pilih berdasarkan tujuan dan kemampuanmu</h1><p>Pilih cara belajar, lalu tentukan level N1–N5</p><p>Semua level dapat dibeli secara bebas. Chapter di dalam level aktif tetap mengikuti urutan belajar yang terstruktur.</p><Link className="button button-primary" href="/placement">Coba Placement Test</Link></section><section className="public-section"><h2>CARA BELAJAR</h2><p className="public-intro">Pilih cara belajar terlebih dahulu</p><div className="public-cards">{plans.map((plan) => <article className="public-card" key={plan.title}><span>{plan.badge}</span><h3>{plan.title}</h3><p>{plan.description}</p><ul>{plan.points.map((point) => <li key={point}>{point}</li>)}</ul></article>)}</div></section><section className="public-section"><h2>LEVEL JOURNEY</h2><p className="public-intro">Pilih level N1–N5 secara bebas</p><div className="public-levels">{levelCatalog.map((level) => <Link className="public-level" href={`/program/${level.code}`} key={level.code}><strong>{level.code}</strong><span>Tersedia</span><h3>{level.name}</h3><p>{level.description}</p><b>→</b></Link>)}</div></section></main></PublicPage>; }
