import Link from "next/link";
import { PublicPage } from "@/components/public-shell";
import { levelCatalog } from "@/lib/public-mock";
export function generateStaticParams() { return levelCatalog.map((level) => ({ level: level.code.toLowerCase() })); }

export default async function ProgramDetailPage({ params }: { params: Promise<{ level: string }> }) { const { level } = await params; const item = levelCatalog.find((entry) => entry.code === level.toUpperCase()) ?? levelCatalog[1]; return <PublicPage active="Program"><main className="public-main"><section className="public-hero"><p className="kicker">CONTOH LEVEL TERPILIH • JLPT {item.code}</p><h1>Pelajari level targetmu melalui journey yang terstruktur</h1><p>Contoh ini menampilkan JLPT {item.code}. Struktur konten menyesuaikan level N1–N5 yang dipilih pengguna.</p><div className="result-actions"><Link className="button button-primary" href="/program">Pilih Program</Link><Link className="button button-dark" href="/placement">Coba Placement</Link></div></section><section className="public-section"><h2>Fasilitas belajar lengkap</h2><p className="public-intro">Setiap chapter menggabungkan pemahaman materi, latihan aktif, dan evaluasi.</p><div className="public-cards">{["Video Penjelasan", "Modul", "Latihan", "Checkpoint", "Flashcard", "Try Out JLPT"].map((title) => <article className="public-card" key={title}><h3>{title}</h3><p>Konten belajar terstruktur dengan progres yang tercatat.</p></article>)}</div></section></main></PublicPage>; }
