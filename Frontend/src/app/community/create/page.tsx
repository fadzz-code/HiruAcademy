import { Suspense } from "react";
import { SupportingRoute } from "@/components/supporting-route";
export default function CreatePostPage() { return <Suspense><SupportingRoute kind="createPost" /></Suspense>; }
