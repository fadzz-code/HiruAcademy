import { Suspense } from "react";
import { SupportingRoute } from "@/components/supporting-route";
export default function CommunityPage() { return <Suspense><SupportingRoute kind="community" /></Suspense>; }
