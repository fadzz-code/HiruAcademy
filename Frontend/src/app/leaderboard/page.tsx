import { Suspense } from "react";
import { SupportingRoute } from "@/components/supporting-route";
export default function LeaderboardPage() { return <Suspense><SupportingRoute kind="leaderboard" /></Suspense>; }
