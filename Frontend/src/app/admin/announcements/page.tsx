"use client";

import Link from "next/link";
import { useMemo, useState } from "react";
import { AdminShell } from "@/components/admin-shell";

type Announcement = { id:string; title:string; message:string; type:string; audience:string; channel:string; publication:string; status:string };
type State = "new" | "draft" | "validation" | "published" | "archive" | "archived" | null;
const fixtures:Announcement[] = [
  { id:"a1", title:"Informasi journey terbaru", message:"Informasi terbaru untuk siswa Hiru.", type:"Info", audience:"Semua student", channel:"In-App", publication:"Publish Sekarang", status:"Published" },
  { id:"a2", title:"Perubahan jadwal cohort", message:"Periksa perubahan jadwal cohort aktif.", type:"Urgent", audience:"Belajar dengan Sensei", channel:"In-App + Email", publication:"Terjadwal", status:"Scheduled" },
  { id:"a3", title:"Materi baru Chapter 4", message:"Materi Chapter 4 siap ditinjau.", type:"Info", audience:"N4 active", channel:"In-App", publication:"Draft", status:"Draft" },
  { id:"a4", title:"Maintenance sistem", message:"Maintenance terjadwal untuk semua pengguna.", type:"Urgent", audience:"Semua pengguna", channel:"In-App + Email", publication:"Terjadwal", status:"Published" },
];

export default function AnnouncementsPage() {
  const [items,setItems] = useState(fixtures);
  const [selectedId,setSelectedId] = useState("a3");
  const [query,setQuery] = useState("");
  const [state,setState] = useState<State>(null);
  const [title,setTitle] = useState(fixtures[2].title);
  const [message,setMessage] = useState(fixtures[2].message);
  const [type,setType] = useState(fixtures[2].type);
  const [audience,setAudience] = useState(fixtures[2].audience);
  const [channel,setChannel] = useState(fixtures[2].channel);
  const [publication,setPublication] = useState(fixtures[2].publication);
  const [start,setStart] = useState("");
  const [previewApproved,setPreviewApproved] = useState(false);
  const [reason,setReason] = useState("");
  const [error,setError] = useState("");
  const visible = useMemo(() => items.filter((item) => `${item.title} ${item.audience}`.toLowerCase().includes(query.toLowerCase())),[items,query]);
  const checks = [Boolean(title.trim() && message.trim()),Boolean(audience),Boolean(channel),publication === "Publish Sekarang" || Boolean(start),previewApproved];
  function select(item:Announcement) { setSelectedId(item.id);setTitle(item.title);setMessage(item.message);setType(item.type);setAudience(item.audience);setChannel(item.channel);setPublication(item.publication);setStart("");setPreviewApproved(false);setState(null); }
  function create() { setSelectedId("new");setTitle("");setMessage("");setType("Info");setAudience("");setChannel("In-App");setPublication("Draft");setStart("");setPreviewApproved(false);setState("new"); }
  function publish() { if (!checks.every(Boolean) || type === "Urgent" && ["Semua student","Semua pengguna"].includes(audience)) {setState("validation");return;} setItems((list) => list.map((item) => item.id === selectedId ? {...item,title,message,type,audience,channel,publication,status:"Published"}:item));setState("published"); }
  function archive() { if (!reason.trim()) {setError("Alasan arsip wajib diisi.");return;} setItems((list) => list.map((item) => item.id === selectedId ? {...item,status:"Archived"}:item));setState("archived"); }

  return <AdminShell current="communication"><main className="admin-page admin-a7-page">
    <header className="admin-header"><div><p className="admin-kicker">ADMIN • COMMUNICATION</p><h1>Announcement Management</h1><p>Kelola audience, channel, priority, schedule, publish state, dan riwayat pengumuman.</p></div><div className="admin-header-actions"><Link className="button button-secondary" href="/admin">Dashboard</Link><button className="button button-primary" type="button" onClick={create}>Buat Pengumuman</button></div></header>
    <section className="admin-kpi-grid">{[["Published","Dinamis","Pengumuman aktif."],["Scheduled","Dinamis","Menunggu waktu publikasi."],["Draft","Dinamis","Belum dipublikasikan."],["Audience reach","Dinamis","Dihitung dari target audience."]].map(([label,value,meta]) => <article className="admin-kpi-card" key={label}><h2>{label}</h2><strong>{value}</strong><small>{meta}</small></article>)}</section>
    <div className="a7-layout"><aside className="a7-list"><header><h2>Daftar pengumuman</h2><label className="admin-search-box"><span aria-hidden="true">⌕</span><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Cari judul atau audience" /></label></header>{visible.map((item) => <button className={selectedId === item.id ? "active":""} type="button" onClick={() => select(item)} key={item.id}><div><strong>{item.title}</strong><small>{item.audience}</small></div><b>{item.status}</b></button>)}<button className="a7-archive-link" type="button" onClick={() => {setReason("");setError("");setState("archive");}}>Lihat Arsip</button></aside>
      <section className="a7-editor"><header><div><p className="admin-kicker">DRAFT • {type.toUpperCase()}</p><h2>Editor pengumuman</h2></div><span>Draft</span></header>{state === "new" && <aside className="a7-editor-note"><strong>PENGUMUMAN</strong><p>Pengumuman baru siap dibuat. Atur audience, isi, jadwal, CTA, dan mode publikasi sebelum disimpan.</p></aside>}<label className="admin-field"><span>Judul</span><input value={title} onChange={(event) => setTitle(event.target.value)} /></label><label className="admin-field"><span>Ringkasan</span><textarea value={message} onChange={(event) => setMessage(event.target.value)} rows={4}/></label><div className="a7-field-grid"><label className="admin-field"><span>Tipe</span><select value={type} onChange={(event) => setType(event.target.value)}><option>Info</option><option>Urgent</option></select></label><label className="admin-field"><span>Audience</span><select value={audience} onChange={(event) => setAudience(event.target.value)}><option value="">Pilih audience</option><option>Semua student</option><option>Level tertentu</option><option>Cohort</option><option>Role: Student</option><option>Role: Admin</option><option>Free Member</option><option>Belajar Mandiri</option><option>Belajar dengan Sensei</option><option>N4 active</option><option>Semua pengguna</option></select></label><label className="admin-field"><span>Channel</span><select value={channel} onChange={(event) => setChannel(event.target.value)}><option>In-App</option><option>Email</option><option>In-App + Email</option></select></label><label className="admin-field"><span>Publikasi</span><select value={publication} onChange={(event) => setPublication(event.target.value)}><option>Draft</option><option>Publish Sekarang</option><option>Terjadwal</option></select></label><label className="admin-field"><span>Mulai</span><input value={start} onChange={(event) => setStart(event.target.value)} placeholder="Tanggal/waktu dari admin" /></label><label className="admin-field"><span>Berakhir (Opsional)</span><input placeholder="Tanggal/waktu dari admin" /></label></div><section className="a7-preview"><p className="admin-kicker">Preview</p><strong>Pengumuman</strong><p>{message || "Isi pengumuman belum tersedia."}</p><label><input type="checkbox" checked={previewApproved} onChange={(event) => setPreviewApproved(event.target.checked)} />Preview disetujui</label></section><section className="a7-checklist"><h3>Publish checklist</h3>{["Judul dan pesan terisi","Audience dipilih","Channel valid","Waktu publikasi valid","Preview disetujui"].map((label,index) => <div className={checks[index] ? "complete":"incomplete"} key={label}><span>{checks[index] ? "✓":"!"}</span><strong>{label}</strong></div>)}</section><aside className="a7-warning"><strong>Pengumuman</strong><p>Pengumuman urgent dan audience besar perlu review sebelum publish.</p></aside><div className="a7-actions"><button className="button button-secondary" type="button" onClick={() => setState("draft")}>Simpan Draft</button><button className="button button-primary" type="button" onClick={publish}>Publish</button></div></section></div>
    {state && !["new"].includes(state) && <div className="admin-dialog-layer"><button className="admin-dialog-backdrop" type="button" onClick={() => setState(null)} aria-label="Tutup state pengumuman"/><section className="a4-dialog" role="dialog" aria-modal="true" aria-labelledby="announcement-state-title">{state === "draft" && <section className="a4-success"><p className="admin-kicker">ADMIN • SIMPAN PENGUMUMAN</p><h2 id="announcement-state-title">Draft pengumuman berhasil disimpan</h2><span>✓</span><p>Audience, isi, jadwal, dan mode publikasi tersimpan pada fixture lokal; backend dan audit belum terhubung.</p><button className="button button-primary" onClick={() => setState(null)} type="button">Kembali ke Pengumuman</button></section>}{state === "validation" && <><p className="admin-kicker">ADMIN • VALIDASI PENGUMUMAN</p><h2 id="announcement-state-title">Pengumuman belum dapat dipublikasikan</h2><p>Lengkapi audience, isi, jadwal, dan mode publikasi sebelum pengumuman diterbitkan. Urgent dengan audience besar memerlukan review.</p><button className="button button-primary" onClick={() => setState(null)} type="button">Periksa Editor</button></>}{state === "published" && <section className="a4-success"><p className="admin-kicker">PENGUMUMAN • PREVIEW</p><h2 id="announcement-state-title">Validasi lokal selesai</h2><span>✓</span><p>Tidak ada pengumuman yang dikirim atau dipublikasikan ke Student/Public.</p><button className="button button-primary" onClick={() => setState(null)} type="button">Kembali</button></section>}{state === "archive" && <><p className="admin-kicker">ADMIN • KONFIRMASI ARSIP</p><h2 id="announcement-state-title">Arsipkan pengumuman ini?</h2><p>Pengumuman akan dipindahkan dari daftar aktif. Riwayat publikasi dan audit tetap disimpan.</p><label className="admin-field"><span>Alasan</span><textarea value={reason} onChange={(event) => setReason(event.target.value)} rows={3}/></label>{error && <p className="a4-validation">{error}</p>}<div><button className="button button-primary" type="button" onClick={archive}>Konfirmasi Arsip</button><button className="button button-secondary" type="button" onClick={() => setState(null)}>Batal</button></div></>}{state === "archived" && <section className="a4-success"><p className="admin-kicker">PENGUMUMAN • ARSIP</p><h2 id="announcement-state-title">Pengumuman dipindahkan ke arsip lokal</h2><p>Fixture tidak dihapus permanen; audit belum dipersistensikan.</p><button className="button button-primary" type="button" onClick={() => setState(null)}>Kembali</button></section>}</section></div>}
  </main></AdminShell>;
}
