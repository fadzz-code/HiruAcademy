import { Suspense } from "react";
import { SupportingRoute } from "@/components/supporting-route";
export default function ProfilePage() { return <Suspense><SupportingRoute kind="profile" /></Suspense>; }
