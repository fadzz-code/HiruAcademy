import { Suspense } from "react";
import { SupportingRoute } from "@/components/supporting-route";
export default function PracticePage() { return <Suspense><SupportingRoute kind="practice" /></Suspense>; }
