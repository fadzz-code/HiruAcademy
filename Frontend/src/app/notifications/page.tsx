import { Suspense } from "react";
import { SupportingRoute } from "@/components/supporting-route";
export default function NotificationsPage() { return <Suspense><SupportingRoute kind="notifications" /></Suspense>; }
